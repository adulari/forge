<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-07-15 — do not edit manually*

### RTK / Tool Output Guardrails
*~1,791 tokens/session saved*
- For `sklearn/linear_model/ridge.py`, avoid repeated narrow `Read` slices around `RidgeClassifierCV`/`store_cv_values`; fetch the full relevant class/function block once with a larger output limit or use symbol/line search first, then one exact range read.
- If a `Read` output is truncated or too small to establish context, do not re-run adjacent slices repeatedly; raise the output limit or read the complete target file/block once.

### Scikit-Learn SWE Bench Environment
*~1,535 tokens/session saved*
- In old scikit-learn SWE bench repos, local imports often fail on missing `sklearn.__check_build` extensions; do not keep retrying `python -c import sklearn`, `pip show`, conda probes, or rebuilds after the first build/import failure.
- For scikit-learn 10297-style fixes, prefer source-level checks and targeted static inspection unless a compatible prebuilt/build environment is already proven available.

### Python Build Commands
*~900 tokens/session saved*
- Avoid `python setup.py build_ext --inplace` loops in legacy scikit-learn under modern Python; it repeatedly fails on old numpy/distutils/Cython compatibility unless dependencies are pinned for that repo first.

<!-- headroom:learn:end -->
