Implement the ordered concurrent Go pipeline in this repository completely. Read README.md and all tests, repair the worker lifecycle, ordering, error/panic attribution, cancellation, and backpressure behavior without weakening tests or changing the public API. Use idiomatic Go, track a concise task list, and finish only after `go test -race ./...` plus fresh formatting/static verification pass.
