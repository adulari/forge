Repair this Rust transaction-ledger crate completely. Read README.md, all source, and all tests; preserve the exact public API while making account construction, batch execution, rollback, checked arithmetic, idempotency/conflict handling, and deterministic receipt ordering satisfy the documented contract. Use only the standard library, do not weaken tests, keep a concise task list, and finish only after fresh `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets` all pass.
