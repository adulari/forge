Repair this offline TypeScript package completely. Read README.md, package/build configuration, source, and tests. Restore the build and package entry point, then implement the documented safe deep-merge contract while preserving the exact public exports and TypeScript API. Do not weaken tests or add dependencies. Track a concise task list and finish only after `npm test` and a fresh strict TypeScript build both pass.
