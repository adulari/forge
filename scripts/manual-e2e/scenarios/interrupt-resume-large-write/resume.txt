Continue the interrupted task safely. Inspect whether `interrupted.txt` exists and whether it is complete. Never treat a partial or malformed tool call as success. Produce the exact requested 321-line file, splitting the work into coherent `write_file` plus `append_file` chunks if that is safer, then verify exact line count and boundary lines. Finish the tracked task only after verification passes.
